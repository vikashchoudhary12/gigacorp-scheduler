# Copy to .streamlit/secrets.toml locally, or paste into Streamlit Cloud's
# "Secrets" settings panel. You only need the key matching whichever
# provider you pick in the sidebar.

GROQ_API_KEY = "gsk_...."
ANTHROPIC_API_KEY = "sk-ant-...."
OPENAI_API_KEY = "sk-...."
